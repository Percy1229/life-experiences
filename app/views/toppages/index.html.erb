<% if logged_in? %>
<div class="container">
    <div class="text-center">
        <h1>Toppages</h1>
    </div>

<%= render 'experiences/experiences', users: @users, experiences: @experiences %>
</div>
<% end %>