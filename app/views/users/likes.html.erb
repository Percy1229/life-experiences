<div class="row">
    <aside class="col-sm-4">
        <div class="card  text-center">
            <div class="card-header">
                <div class="card-title">
                <span><i class="<%= @crown %>"></i></span>
                <%= @user.name %>
            </div>
        </div>
            <div class="card-body">
                <div>
                <% if @user.img? %>
                <%= image_tag(@user.img_url, width: 100, height: 100, class: 'rounded-circle') %>
                <% else %>
                <%= image_tag('http://res.cloudinary.com/hhzeaawuf/image/upload/default.png', width: 80, height: 80) %>
                <% end %>
                </div><br>
                <%= @user.introduce %><br>
                <% if current_user == @user %>
                <%= link_to 'Edit Profile', edit_user_path(current_user) %>
                <% end %>
                <p class="pt-1 favorite"><span><i class="far fa-handshake favorite"></i> </span><%= @count_liked %></p>
            </div>
        </div>
    </aside>
    <div class="col-sm-8">
        <ul class="nav nav-tabs nav-justified mb-3">
            <li class="nav-item">
            <a href="<%= user_path(@user) %>" 
            class="nav-link <%= 'active' if current_page?(user_path(@user)) %>"
            ><%= @user.name %>'s post
            </a>
            </li>
            
            <li class="nav-item">
                <a href="<%= likes_user_path(@user) %>"
                class="nav-link <%= 'active' if current_page?(likes_user_path(@user)) %>">
                    <%= @user.name %>'s favorite
                </a>
            </li>
        </ul>
<%= render 'experiences/experiences', users: @likes, experiences: @experiences, liked: @liked %>
    </div>
</div>
