<div class="row">
    <aside class="col-sm-4">
        <div class="card">
            <div class="card-header">
                <img href="#">
            </div>
            <div class="card-body">
            <div class="card-title text-center">
                <%= @user.name %>
            </div>
            </div>
        </div>
    </aside>
    <div class="col-sm-8">
        <ul class="nav nav-tabs nav-justified mb-3">
            <li class="nav-item">
            <a href="<%= user_path(@user) %>" 
            class="nav-link <%= 'active' if current_page?(user_path(@user)) %>"
            ><%= @user.name %>'s post
            </a>
            </li>
            
            <li class="nav-item"><a href="#" class="nav-link"><%= @user.name %>'s favorite</a></li>
        </ul>
<%= render 'experiences/experiences', experiences: @experiences %>
    </div>
</div>
